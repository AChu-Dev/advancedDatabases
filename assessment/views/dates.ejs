<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Date</title>
</head>
<body>
    <ul>
        <li> 
            <% try { %>
                <% dates.forEach(function(date){ %> 
                    <li><%= date.Date %></li>
                            <% }); %>
            <% } catch(err) { %>
                <h1><%= err %></h1> 
            <% }; %></li>
    </ul>
</body>
